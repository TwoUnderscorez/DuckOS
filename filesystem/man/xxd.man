NAME
        xxd - make a hexdump.

SYNOPSIS
        xxd [FILE]
        xxd [FILE] [LENGTH] [OFFSET]

DESCRIPTION
        xxd creates a hex dump of a given file.
        LENGTH and OFFSET must be integers in base 10.